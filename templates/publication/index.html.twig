{% extends 'layout.html.twig' %}

{% block title %}{{ parent() }} | Vous souhaitez la bienvenue{% endblock %}

{% block body %}


    <main class="container-fluid mt-3">


        <h1 class="text-center my-5">Actualités</h1>

        <div class="row mx-5">
                <div class="col-lg-12">
                    <div class="card my-4">
                        <h5 class="card-header bg-primary text-center">{{ publication.title }}</h5>
                        <div class="card-body">
                            <img src="{{ publication.picture }}" alt="{{ publication.title }}">
                            <p class="card-text ">{{ publication.description }}</p>
                            <p class="text-muted text-right">{{ publication.date is empty ? '' : publication.date|date('d/m/Y') }}</p>
                        </div>
                    </div>
                </div>
            <div>

            </div>


    </main>

{% endblock %}

