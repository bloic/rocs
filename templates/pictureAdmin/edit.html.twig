{% extends 'layoutAdmin.html.twig' %}

{% block title %}{{ parent() }} | modifier une photo{% endblock %}

{% block body %}
    <main class="container">
        <h1 class="my-5 text-center">Modifier une photo</h1>

        {% include('pictureAdmin/_form.html.twig') with {'label_button':'modifier'} %}

        <a class="btn btn-outline-danger my-4" href="{{ path('admin_picture_index') }}">Accueil administration</a>

        {{ include('pictureAdmin/_delete_form.html.twig') }}
    </main>
{% endblock %}
